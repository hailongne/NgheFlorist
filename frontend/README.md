# NgheFlorist frontend (Vite + React + TypeScript)

Quick start:

```bash
cd frontend
npm install
npm run dev
```

The dev server proxies `/api` to `http://localhost:4000` by default (see `vite.config.ts`).
